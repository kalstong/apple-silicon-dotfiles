[core]
	pager = delta --24-bit-color auto --dark --line-numbers --keep-plus-minus-markers --syntax-theme ansi

[init]
	defaultBranch = main

[interactive]
	diffFilter = delta --24-bit-color auto --dark --line-numbers --diff-so-fancy --keep-plus-minus-markers --syntax-theme ansi

[pull]
	rebase = true

[push]
	autoSetupRemote = true

[user]
	name = GIT_USER_NAME
	email = GIT_USER_EMAIL
